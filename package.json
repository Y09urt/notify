{
  "name": "cloudflare-mobile-notify",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "db:init": "wrangler d1 execute notify-db --file=./schema.sql",
    "queue:create": "wrangler queues create notify-push"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260522.0",
    "typescript": "^5.9.3",
    "wrangler": "^4.18.0"
  }
}
